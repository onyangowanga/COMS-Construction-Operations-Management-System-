# Application Logs Directory

This directory contains application log files.

Logs are generated by:
- Django application
- Gunicorn web server
- Deployment scripts
- Background tasks

**Note:** Log files (*.log) are excluded from git to keep repository size small.

View logs on VPS:
```bash
cd /root/coms
docker-compose -f docker-compose.prod.yml logs -f web
```
